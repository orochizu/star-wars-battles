import styled from '@emotion/styled';
import { Fab } from '@material-ui/core';

const FixedFax = styled(Fab)`
    position: fixed;

    bottom: 15px;
    left: 50%;
    transform: translate(-50%, 0);
`;

export default FixedFax;
